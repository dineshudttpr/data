GIT Commands 

 git  init  - create a new reporitory  - convert the folder into the repoistory 

 git add 

git commit -m "Commit Message"   

git status

git config  

git config  --global user.name "Dinesh"
git config  --global user.email "dinesh@gmail.com"

current repoistory 
git config user.name "Dinesh"
git config  user.email "dinesh@gmail.com"

 
 git branch  

  git branch  -a

 git checkout  

 git 
